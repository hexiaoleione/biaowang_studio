package com.hex.express.iwant.utils;
import android.content.Context;
import com.hex.express.iwant.constance.CollectionKey;

/**
 * 
 * 微信工具类
 * @author Ran
 *
 * 时间: 2016年3月19日
 */
public class WXUtils {
	/**分享*/
	
	/**注册*/
	/*public static void  regist(Context context){
		IWXAPI api = WXAPIFactory.createWXAPI(context, CollectionKey.WXAPP_KEY);
	    if (!api.isWXAppInstalled()) {
	    	ToastUtil.shortToast(context, "您好没有安装微信");
            return;
        }
		api.registerApp( CollectionKey.WXAPP_KEY);
		SendAuth.Req req = new SendAuth.Req();
	    req.scope = "snsapi_userinfo";//获取用户信息填写snsapi_userinfo
	    req.state = "wechat_sdk_demo_test";
	    api.sendReq(req);
	}*/
	
}
